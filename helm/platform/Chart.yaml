apiVersion: v2
name: platform
description: Cloud DWH control plane
type: application
version: 0.1.0
appVersion: "0.1.0"
# No external chart deps — postgres/redis are local templates (offline-friendly)
